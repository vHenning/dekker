Generates a state diagram of Dekkers algorithm as discussed in Montana State University course EELE565.
Checks generated diagram for mututal exclusion violations, deadlocks and starvation.

Just execute main.py and look for log output and graph.png in the same directory.